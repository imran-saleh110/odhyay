Functions that talk to APIs.

services/
    api.js
    auth.js
    users.js

Instead of writing

`fetch(...)`

inside components, you do

`const users = await getUsers();`

The component doesn't need to know where the data comes from.
